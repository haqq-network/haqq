package types

const (
	// ModuleName defines the module name
	ModuleName = "contractcheck"

	// StoreKey defines the primary module store key
	StoreKey = ModuleName

	RouterKey = ModuleName
)

const (
	prefixDenom = iota + 1
	denomCounter
)
