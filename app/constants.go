package app

const (
	// Name defines the application name
	Name = "haqq"
	// DaemonName defines the application binary name
	DaemonName = Name + "d"
	// DirName defines the default application directory name
	DirName = "." + DaemonName
	// MainnetChainID defines the chain ID of the mainnet
	MainnetChainID = "haqq_11235"
)
